import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
    components: {
        Layout : {
            headerBg :"#ffffff",
        }
    },
    token: {
        colorPrimary: "#277eff",
    },
};

export default theme;